import { StyledTicketTitle, StyledTicketWrapper } from './Ticket.styled';

export const Ticket = () => {
    return (
        <StyledTicketWrapper elevation={1}>
            <StyledTicketTitle>bug</StyledTicketTitle>
        </StyledTicketWrapper>
    );
};
