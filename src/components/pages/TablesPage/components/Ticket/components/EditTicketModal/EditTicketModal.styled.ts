import { styled } from '@mui/material';
import { Box } from '@mui/system';

export const StyledEditTicketModalWrapper = styled(Box)(({ theme }) => ({
    display: 'flex',
    flexDirection: 'column',
}));
